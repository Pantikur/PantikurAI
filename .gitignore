# === Python ===
__pycache__/
*.py[cod]
*.so
.Python
python-build-stamp*
pip-log.txt
pip-delete-this-directory.txt
.tox/
.venv/
venv/
env/
.virtualenv/
.pytest_cache/
pytestdebug.log
.hypothesis/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover

# === Системные ===
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
ehthumbs_vista.db

# === IDE ===
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*~

# === Данные и модели — СНАЧАЛА запретить, ПОТОМ разрешить конкретные ===
data/
!data/tokenizer.json
!data/chat_data.pkl
!data/config.yaml

models/
!models/model.pth

# === Прочие артефакты — игнорируем полностью ===
*.joblib
*.bin
*.safetensors
outputs/
checkpoints/
runs/

# === Логи ===
logs/
*.log

# === Временные файлы ===
*.tmp
*.temp

# === Окружения (не копируем в Docker) ===
.env
.env.local
.env.development
.env.production
.env.staging
.env.test

# === Docker ===
docker-compose.override.yml

# === CI/CD (опционально) ===
.gitlab-ci.yml
azure-pipelines.yml
