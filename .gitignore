# === Python ===
__pycache__/
*.py[cod]
*.so
.Python
python-build-stamp*
pip-log.txt
pip-delete-this-directory.txt
.tox/
.venv/
venv/
env/
.virtualenv/
.pytest_cache/
pytestdebug.log
.hypothesis/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover

# === Системные ===
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
ehthumbs_vista.db

# === IDE ===
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*~

# === МОДЕЛИ И ДАННЫЕ (ВСЕ БОЛЬШИЕ ФАЙЛЫ ЗАПРЕЩЕНЫ) ===
# ✅ Разрешён только tokenizer.json (маленький)
data/*.jsonl
data/*.pkl
data/*.joblib
data/*.bin
data/*.safetensors
models/*.pt
models/*.pth
models/*.joblib
models/*.bin
models/*.safetensors
models/*.onnx
models/*.gguf
models/qwen2.5-3b/  # Локальная модель Qwen (6 ГБ)
models/image_models/  # Модели для генерации изображений (Айко)

# Исключение: tokenizer.json разрешён
!data/tokenizer.json
!data/conversations.json
!data/training_pairs.jsonl

# README для моделей разрешён
models/README.md

# === World Engine ===
data/worlds/
data/books/books_cycle_*.jsonl

# === Артефакты (полный запрет) ===
*.joblib
*.bin
*.safetensors
outputs/
checkpoints/
runs/
*.tmp
*.temp

# === Логи ===
logs/
*.log
logs/*.log

# === Окружения ===
.env
.env.local
.env.development
.env.production
.env.staging
.env.test

# === Docker ===
docker-compose.override.yml

# === CI/CD (опционально) ===
.gitlab-ci.yml
azure-pipelines.yml

# === PyTorch (доп. защита) ===
venv/Lib/site-packages/torch/lib/*.dll
venv/Lib/site-packages/torch/lib/*.so
venv/Lib/site-packages/torch/lib/*.dylib
# === Gradle ===
gradle-8.5/
gradle-8.5-bin.zip
.gradle/

# === Android Studio Plugin ===
android-studio-plugin/build/
android-studio-plugin/.gradle/

# === Temp files ===
temp_*.txt
test_request.json
data/.last_retrain_timestamp

# === Временные тесты и логи ассистента ===
.koda_tmp/
app-logs-*.txt

# === Chat (не нужно в репозитории) ===
PantikurChat/
Pantikur/

# === Тестовые файлы ===
test_*.png

# === Runtime-данные модулей (state, логи, кэш) ===
# Состояние всех инженеров
**/engine/state/
**/engine/state/*.json
**/engine/state/*.txt
**/engine/state/*.pkl

# Бэкапы
**/backups/

# Отчёты и ML-оптимизация
**/analysis_report.json
**/ml_optimization_report.json
**/ml_optimization_report.txt
**/project_report.txt

# Глобальные временные данные
global_progress.json
auto_start.json
events.json
knowledge_level.json
messages.json
my_character.json
my_character.yaml
generated_worlds.json
